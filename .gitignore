# Dependencies
node_modules/
.pnp
.pnp.js

# Environment — NEVER commit real secrets. Only .env.example is tracked.
.env
.env.local
.env.*.local
!.env.example

# Build output
dist/
build/
out/
.next/
next-env.d.ts
*.tsbuildinfo

# Prisma generated client (regenerated by `npm run prisma:generate`)
apps/api/generated/
apps/api/node_modules/.prisma/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing / coverage
coverage/
.nyc_output/

# Editors & OS
.vscode/*
!.vscode/extensions.json
.idea/
# Claude Code per-machine settings (the non-.local settings.json IS shared)
.claude/settings.local.json
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# Local uploads (documents are stored in object storage in real environments —
# this folder is only a placeholder for local development)
uploads/
